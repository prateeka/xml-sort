package com.prateek.xmlcompare
import org.scalatest.funspec.AnyFunSpec

class MainTest extends AnyFunSpec {
  describe("Main execution") {
    describe("when request files include both indented and single lined") {
      describe(
        "when some request files have counterpart matches and some do not"
      ) {
        it("should have 2 matching and 1 non-matching") {
          assert()
        }
      }
    }
  }
}
